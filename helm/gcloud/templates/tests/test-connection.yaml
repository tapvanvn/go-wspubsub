apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ws-pubsub.fullname" . }}-test-connection"
  labels:
    {{- include "ws-pubsub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ws-pubsub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
